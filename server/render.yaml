services:
  - type: web
    name: moviebox-proxy
    env: python
    region: oregon # us-west-2, free tier region
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
